# Antigravity Context: KOI1 Encinitas

> **ESTE ARCHIVO ES EL GATILLO (TRIGGER) DE SESION.**
> Snapshot operativo del estado actual del repo Docker/MySQL.
> No reemplaza memoria persistente ni documentación técnica detallada.
>
> **Bootstrap recomendado:**
> 1. Leer este archivo.
> 2. Leer `docs/protocolo-agentes.md`.
> 3. Leer `docs/INDEX.md`.
> 4. Leer la doc del área afectada en `docs/`.
>
> **Al finalizar cada sesión:**
> 1. Actualizar este archivo con el estado actual.
> 2. Crear o actualizar la doc correspondiente en `docs/`.
> 3. Registrar explícitamente si el cambio aplica a:
>    - `encinitas_prod_truth` (MSSQL / producción Windows)
>    - `encinitas` Docker (`192.168.2.210:8195`, PHP 5.6 / MySQL 8)

---

## Estado Actual (2026-05-12)

### Entornos
- **Producción KOI1 real:** `\\SERVER\htdocs\encinitas`
- **Fuente de verdad para análisis legacy:** `C:\dev\encinitas_prod_truth`
- **Docker KOI1 MySQL:** `C:\dev\encinitas`
- **Host Docker:** `192.168.2.210`
- **URL Docker:** `http://192.168.2.210:8195/`
- **Bind mount Docker:** `/var/www/encinitas`
- **Contenedor:** `koi1-php56`

### Regla operativa clave
- No mezclar cambios de `encinitas_prod_truth` con `encinitas` sin verificar compatibilidad de runtime.
- `encinitas_prod_truth` asume SQL Server / `EXEC`.
- `encinitas` Docker asume MySQL 8 / `CALL`.
- No hacer `reset --hard origin/main` en el server Docker sin validar primero los commits locales del host.

### Última sesión relevante
- Se corrigió `gestion_cobranza` en Windows/prod truth para consistencia de `saldoFechaHasta`, `empresa` y export.
- Se corrigió `gestion_proveedores` en Docker/MySQL para mostrar `Saldo a la fecha`.
- Se detectó que el error al buscar con fecha en Docker provenía de la capa común:
  - `Factory::getArrayFromStoredProcedure()`
  - `Factory::getListObjectFromStoredProcedure()`
  seguían armando SQL Server `EXEC ...` a través de `Mapper`, en vez de usar `Datos::EjecutarStoredProcedure()` con `CALL ...`.
- Se restauró el server Docker al commit funcional `ac54b05` antes de portar cambios selectivos.

### Riesgos activos
- Los módulos que usan stored procedures en Docker/MySQL pueden fallar si pasan por rutas legacy con `EXEC`.
- El server Ubuntu puede tener commits locales no publicados; revisar antes de sincronizar con GitHub.
- En KOI1 legacy hay diferencias de encoding y comportamiento entre Windows/MSSQL y Docker/MySQL.

### Pendientes activos
- [ ] Validar en Docker `gestion_proveedores` con búsqueda real usando `saldoFechaHasta`
- [ ] Portar a Ubuntu solo cambios compatibles con el baseline funcional del host
- [ ] Documentar módulos legacy adaptados a MySQL (`CALL`, collation, aliases, IDs)

