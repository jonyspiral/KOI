CREATE OR REPLACE VIEW `Ordenes_compra_detalle_v` AS SELECT d.*, c.cod_proveedor, c.fecha_emision, c.es_hexagonoFROM Ordenes_compra_cabecera cINNER JOIN Ordenes_compra_detalle d ON c.cod_sucursal = d.cod_sucursal AND c.nro_orden_compra = d.nro_orden_comp;